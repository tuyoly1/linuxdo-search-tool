# Linux.do Search Tool Mobile

这是 `linuxdo-search-tool` 的手机优化版。

## 设计目标

- 更短的首屏路径
- 更少的默认按钮
- 更适合单手操作
- 优先把推荐入口顶到最前面

## 和桌面版的区别

- 高级筛选默认折叠
- 顶部直接放 `打开推荐`
- 卡片默认只显示推荐入口、Bing 备用和一个站内入口
- 优先入口单独放在前面
- 高级筛选中支持自定义排除词和主题模式
- 布局完全按手机竖屏优先设计

## 打开方式

直接打开：

```text
mobile/index.html
```
